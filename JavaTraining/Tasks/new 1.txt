принимает массив чаров, возвращает строку состоящую из символов массива
принимает массив интов, и значение типа инт, возвращает индекс массива в котором значение совпадает с передаваемым, начиная с начала массива. Если значения в массиве нет возвращает -1
принимает массив интов, и значение типа инт, возвращает индекс массива в котором значение совпадает с передаваемым, начиная с конца массива. Если значения в массиве нет возвращает -1
принимает инт год, и возвращает тру если год високосный
принимает массив интов и число, выводит на экран только элементы массива которые кратны этому числу
метод принимает массив интов сортирует его по возрастанию
принимает массив байт, если в массиве есть повторяющиеся элементы, возвращает тру

Написать 2 метод для конвертации :
int -> String
double -> String
1) Нельзя использовать стандартные методы для конвертации.
2) При контеканации можно за раз использовать не больше 1 символа (т.е. при переводе из числа в строку, из числа нужно достать по одной цифре)
3) Числа могут быть отрицательными
4) Числа с плавающей точкой могут и не содержать дробную часть (12345)
п.с. Можно использовать методы класса Math

import java.util.*;

class GameOne {
	public static void main(String [] args){
		int [] array = generateRandomArray(5);
		print(array);
		String str = "hello";
		char c = str.charAt(4);
		System.out.println(str.indexOf("ll"));
		System.out.println(Math.abs(-10000));
	}	
	
	static void print(int [] array) {
		for(int i = 0; i < array.length; i++){
			System.out.println(array[i]);
		}
	}
	
	static int[] generateRandomArray(int size){
		Random random = new Random();
		int [] array = new int[size];
		for(int i = 0; i < array.length; i++){
			array[i] = 60 + random.nextInt(100);
		}
		return array;
	}
}